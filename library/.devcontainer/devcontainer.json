{
	"name": "Library DevContainer",
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"workspaceMount": "source=${localWorkspaceFolder},target=/home/dev/app,type=bind",
	"workspaceFolder": "/home/dev/app",
	"remoteUser": "dev",
	"postCreateCommand": "git config --global --add safe.directory /home/dev/app && git config --global commit.gpgSign false",
	"mounts": ["source=${localWorkspaceFolder}/.git,target=/home/dev/app/.git,type=bind,consistency=cached"]
}
